```js
webpack配置采用commonJS规范，通过module.export导出一个秒速如何构建的Object对象

module.export = {
  // 入口配置
  entry: {},
  // 出口配置
  output: {},
  // module.rules loader
  modules: {},
  // 插件
  plugins: [],
  // 开发服务器
  devServer: {}
};

```